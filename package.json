{
  "scripts": {
    "start": "http-server ./docs -o -c-1",
    "start:w": "nodemon --watch 'docs' --ext 'yaml' --exec npm start"
  },
  "dependencies": {
    "http-server": "^0.12.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.2"
  }
}
